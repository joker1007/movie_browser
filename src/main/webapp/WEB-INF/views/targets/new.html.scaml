-@val s: skinny.Skinny

- import controller.TargetsController

%h3 #{s.i18n.get("target.new")}
%hr

-#-for (e <- s.errorMessages)
-#  %p(class="alert alert-danger") #{e}

%form(method="post" action={url(TargetsController.createUrl)} class="form")
  =include("_form.html.scaml")
