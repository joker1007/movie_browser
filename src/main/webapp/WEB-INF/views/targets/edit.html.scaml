-@val s: skinny.Skinny

- import controller.TargetsController

%h3 #{s.i18n.get("target.edit")}
%hr

-#-for (e <- s.errorMessages)
-#  %p(class="alert alert-danger") #{e}

%form(method="post" action={url(TargetsController.updateUrl, "id" -> s.params.id.get.toString)} class="form")
  =include("_form.html.scaml")
