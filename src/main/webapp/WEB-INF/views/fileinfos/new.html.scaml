-@val s: skinny.Skinny

%h3 #{s.i18n.get("fileinfo.new")}
%hr

-for (e <- s.errorMessages)
  %p(class="alert alert-danger") #{e}

%form(method="post" action={url(FileinfosController.createUrl)} class="form")
  =include("_form.html.scaml")
